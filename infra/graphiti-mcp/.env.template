# Graphiti MCP configuration
# Replace placeholder values before running `scripts/start-memory.sh`

# Primary LLM / embedder API key (required)
OPENAI_API_KEY=REPLACE_ME_WITH_OPENAI_KEY

# Optional alternative providers (leave empty if unused)
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
GROQ_API_KEY=
VOYAGE_API_KEY=

# Namespace memories for this repo only
GRAPHITI_GROUP_ID=kompass-memory

# Tune concurrency according to your LLM quota (see Graphiti README)
SEMAPHORE_LIMIT=10

# Disable anonymous telemetry if desired (true/false)
GRAPHITI_TELEMETRY_ENABLED=false
