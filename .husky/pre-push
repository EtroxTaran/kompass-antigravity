#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🔍 Running pre-push checks..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# Get current branch name
BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD)

# ============================================================================
# 1. Check branch naming convention
# ============================================================================
echo ""
echo "1️⃣  Checking branch name format..."

# Allow develop, main, and properly formatted feature branches
if [ "$BRANCH_NAME" != "develop" ] && [ "$BRANCH_NAME" != "main" ]; then
  # Check if branch follows naming convention
  if ! echo "$BRANCH_NAME" | grep -qE "^(feature|bugfix|hotfix|refactor|docs|test|chore)/KOM-[0-9]+-[a-z0-9-]+$"; then
    echo "❌ Branch name does not follow naming convention"
    echo "   Got: $BRANCH_NAME"
    echo ""
    echo "   Required format: <type>/KOM-###-<description>"
    echo "   Examples:"
    echo "     feature/KOM-123-customer-validation"
    echo "     bugfix/KOM-456-invoice-fix"
    echo "     hotfix/KOM-789-security-patch"
    echo ""
    echo "   Or rename your branch:"
    echo "     git branch -m <new-branch-name>"
    echo ""
    exit 1
  fi
  echo "   ✅ Branch name is valid: $BRANCH_NAME"
else
  echo "   ✅ Main branch: $BRANCH_NAME"
fi

# ============================================================================
# 2. Check commit messages have Linear issue IDs
# ============================================================================
echo ""
echo "2️⃣  Checking commit messages for Linear issue IDs..."

# Detect first push (no upstream yet) to avoid failing on @{u}
if ! git rev-parse --abbrev-ref @{u} >/dev/null 2>&1; then
  echo "   ℹ️  No upstream found for this branch yet - skipping commit message check on first push"
else
  # Get commits that would be pushed
  COMMITS=$(git log @{u}.. --format=%s 2>/dev/null)

  if [ -z "$COMMITS" ]; then
    echo "   ℹ️  No new commits to push"
  else
    # Check if all commits have Linear issue ID (KOM-###)
    INVALID_COMMITS=$(echo "$COMMITS" | grep -vE "^(feat|fix|docs|style|refactor|perf|test|chore|ci|revert)\(KOM-[0-9]+\):" || true)
    
    if [ -n "$INVALID_COMMITS" ]; then
      echo "   ❌ Some commits missing Linear issue ID:"
      echo "$INVALID_COMMITS" | head -5
      echo ""
      echo "   Required format: <type>(KOM-###): <subject>"
      echo "   Example: feat(KOM-123): add customer validation"
      echo ""
      echo "   Fix with:"
      echo "     git commit --amend  # to fix last commit"
      echo "     git rebase -i       # to fix older commits"
      echo ""
      exit 1
    fi
    echo "   ✅ All commits have Linear issue IDs"
  fi
fi

# ============================================================================
# 3. Check if documentation needs updating (non-blocking warning only)
# ============================================================================
echo ""
echo "3️⃣  Checking documentation updates..."

# Skip documentation diff check on first push (no upstream)
if ! git rev-parse --abbrev-ref @{u} >/dev/null 2>&1; then
  echo "   ℹ️  No upstream found for this branch yet - skipping documentation update check on first push"
else
  # Get changed files compared to upstream
  CHANGED_FILES=$(git diff --name-only @{u}.. 2>/dev/null)

  if [ -n "$CHANGED_FILES" ]; then
    # Check if API changes were made
    if echo "$CHANGED_FILES" | grep -qE "apps/backend/src/.*\.(controller|dto|guard|module)\.ts$"; then
      # Check if docs were updated
      if ! echo "$CHANGED_FILES" | grep -qE "^docs/(api/|specifications/API)"; then
        echo "   ⚠️  INFO: API changes detected but no documentation updates found"
        echo "   Consider updating (non-blocking):"
        echo "     - docs/api/reference/"
        echo "     - docs/specifications/API_SPECIFICATION.md"
        echo "   (Push will continue regardless)"
      else
        echo "   ✅ Documentation updated with API changes"
      fi
    else
      echo "   ✅ No API changes requiring documentation"
    fi
  else
    echo "   ℹ️  No file changes to check"
  fi
fi

# ============================================================================
# 4. Run lint, type-check, and tests (same as CI)
# ============================================================================
echo ""
echo "4️⃣  Running lint (full monorepo)..."

pnpm lint

if [ $? -ne 0 ]; then
  echo ""
  echo "   ❌ Lint failed. Push aborted."
  echo "   Fix lint errors or skip with: git push --no-verify"
  echo ""
  exit 1
fi

echo "   ✅ Lint passed"

echo ""
echo "5️⃣  Running Prettier format check (read-only, matches CI)..."

pnpm format:check

if [ $? -ne 0 ]; then
  echo ""
  echo "   ❌ Format check failed. Push aborted."
  echo "   Run 'pnpm format' to auto-fix formatting issues."
  echo "   Or skip with: git push --no-verify"
  echo ""
  exit 1
fi

echo "   ✅ Format check passed"

echo ""
echo "6️⃣  Running type-check..."

pnpm type-check

if [ $? -ne 0 ]; then
  echo ""
  echo "   ❌ Type-check failed. Push aborted."
  echo "   Fix type errors or skip with: git push --no-verify"
  echo ""
  exit 1
fi

echo "   ✅ Type-check passed"

echo ""
echo "7️⃣  Running unit tests with coverage thresholds (matches CI)..."

# Build shared package first (required for type-check and tests)
echo "   Building shared package..."
pnpm --filter @kompass/shared build

if [ $? -ne 0 ]; then
  echo ""
  echo "   ❌ Shared package build failed. Push aborted."
  exit 1
fi

# Detect which packages changed (use Turbo to test only changed packages)
echo "   Detecting changed packages..."
CHANGED_PACKAGES=""

# Check if upstream exists (not first push)
if git rev-parse --abbrev-ref @{u} >/dev/null 2>&1; then
  # Check if backend changed
  if git diff --name-only @{u}.. 2>/dev/null | grep -qE "^apps/backend/|^packages/shared/"; then
    CHANGED_PACKAGES="$CHANGED_PACKAGES @kompass/backend"
  fi

  # Check if frontend changed
  if git diff --name-only @{u}.. 2>/dev/null | grep -qE "^apps/frontend/|^packages/shared/"; then
    CHANGED_PACKAGES="$CHANGED_PACKAGES @kompass/frontend"
  fi
else
  # First push - test all packages (safety check)
  echo "   ℹ️  First push detected - running all tests"
  CHANGED_PACKAGES="@kompass/backend @kompass/frontend"
fi

# If no packages changed, test all (safety check)
if [ -z "$CHANGED_PACKAGES" ]; then
  echo "   ℹ️  No package changes detected - running all tests"
  CHANGED_PACKAGES="@kompass/backend @kompass/frontend"
else
  echo "   📦 Testing changed packages:$CHANGED_PACKAGES"
fi

# Run tests for changed packages using Turbo
TEST_STATUS=0
for PACKAGE in $CHANGED_PACKAGES; do
  if [ "$PACKAGE" = "@kompass/backend" ]; then
    echo "   Running backend tests with coverage..."
    pnpm --filter @kompass/backend test:cov
    if [ $? -ne 0 ]; then
      TEST_STATUS=1
    fi
  elif [ "$PACKAGE" = "@kompass/frontend" ]; then
    echo "   Running frontend tests with coverage..."
    pnpm --filter @kompass/frontend test:coverage
    if [ $? -ne 0 ]; then
      TEST_STATUS=1
    fi
  fi
done

if [ $TEST_STATUS -ne 0 ]; then
  echo ""
  echo "   ❌ Tests failed or coverage thresholds not met. Push aborted."
  echo "   Fix failing tests or improve coverage to meet thresholds."
  echo "   Coverage requirements: 80% global, 90% for services."
  echo "   Or skip with: git push --no-verify"
  echo ""
  exit 1
fi

echo "   ✅ Tests passed with coverage thresholds met"

echo ""
echo "8️⃣  Running security audit (non-blocking, informational only)..."

pnpm audit --audit-level=high

if [ $? -ne 0 ]; then
  echo ""
  echo "   ⚠️  WARNING: Security audit found high or critical vulnerabilities."
  echo "   This is non-blocking, but please review and fix when possible:"
  echo "     Run 'pnpm audit --fix' to attempt automatic fixes."
  echo "     Or review with: pnpm audit"
  echo ""
  echo "   Push will continue, but please address security issues soon."
  echo ""
else
  echo "   ✅ Security audit passed"
fi

# ============================================================================

# ============================================================================
# Summary
# ============================================================================
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ALL PRE-PUSH CHECKS PASSED"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "   ✅ Branch name valid"
echo "   ✅ Commit messages valid"
echo "   ℹ️  Documentation check completed (non-blocking)"
  echo "   ✅ Lint passed"
echo "   ✅ Format check passed"
echo "   ✅ Type-check passed"
echo "   ✅ Tests passed with coverage thresholds"
echo "   ℹ️  Security audit completed (non-blocking)"
echo ""
echo "   Pushing to $BRANCH_NAME..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
