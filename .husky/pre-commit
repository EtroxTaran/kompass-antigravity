#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🔍 Running pre-commit checks..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# ============================================================================
# 1. Check root directory organization
# ============================================================================
echo ""
echo "1️⃣  Checking root directory organization..."

# Check if any documentation files are being added to root
STAGED_ROOT_FILES=$(git diff --cached --name-only --diff-filter=A | grep -E "^[^/]+\.md$" | grep -vE "^(README|CHANGELOG|LICENSE|CONTRIBUTING)\.md$" || true)

if [ -n "$STAGED_ROOT_FILES" ]; then
    echo "   ❌ Forbidden documentation files in root directory:"
    echo "$STAGED_ROOT_FILES" | while read file; do
        echo "      - $file"
    done
    echo ""
    echo "   📋 RULE: Only these .md files allowed in root:"
    echo "      - README.md"
    echo "      - CHANGELOG.md"
    echo "      - LICENSE"
    echo "      - CONTRIBUTING.md"
    echo ""
    echo "   📁 Move documentation to docs/ subdirectories:"
    echo "      - Implementation reports → docs/implementation/"
    echo "      - Deployment docs → docs/deployment/"
    echo "      - Developer guides → docs/guides/"
    echo "      - API docs → docs/api/"
    echo ""
    exit 1
fi

echo "   ✅ No forbidden files in root directory"

# ============================================================================
# 2. Check for forbidden file patterns (backup, old, etc.)
# ============================================================================
echo ""
echo "2️⃣  Checking for forbidden file patterns..."

FORBIDDEN_PATTERNS=$(git diff --cached --name-only | grep -E "(_backup|\.backup|\.bak|_old|\.old|_v[0-9]|\.v[0-9]|_fixed|\.fixed|_copy|\.copy|_temp|\.temp|~$|\.swp$|\.tmp$)" || true)

if [ -n "$FORBIDDEN_PATTERNS" ]; then
    echo "   ❌ Forbidden file patterns detected:"
    echo "$FORBIDDEN_PATTERNS" | while read file; do
        echo "      - $file"
    done
    echo ""
    echo "   📋 RULE: Use Git for versioning, not file copies"
    echo "      ❌ NEVER create: *_backup*, *.old, *_v2*, etc."
    echo "      ✅ INSTEAD use: git commit, git branch, git tag"
    echo ""
    echo "   See: .cursor/rules/git-workflow.mdc"
    echo ""
    exit 1
fi

echo "   ✅ No forbidden file patterns found"

# ============================================================================
# 3. Check for temporary documentation files
# ============================================================================
echo ""
echo "3️⃣  Checking for temporary documentation files..."

# Temporary file patterns (from .cursor/rules/temporary-files-prevention.mdc)
TEMP_PATTERNS=$(git diff --cached --name-only | grep -E "(LINEAR_ISSUE|LINEAR_ISSUES|linear-issue|AUDIT|AUDIT_FINDINGS|FINDINGS|PLANNING_PROMPT|PLANNING|STATUS_|_STATUS_|BUG_FIX|BUG_FIX_SUMMARY|SEARCH_RESULTS|RESULTS|ANALYSIS|ANALYZ|DESIGN_DOCUMENTS|DESIGN|IMPLEMENTATION_NOTES|NOTES|CLEANUP|_CLEANUP|validation-report|FINAL_DELIVERABLES)" | grep -E "\.md$" || true)

# Exceptions (permanent files that match patterns)
EXCEPTIONS="IMPLEMENTATION_SUMMARY|INTEGRATION_SUMMARY|IMPLEMENTATION_COMPLETE|IMPLEMENTATION_GUIDE|INTEGRATION_GUIDE|ROADMAP|STRATEGY|SPECIFICATION|SPEC|ARCHITECTURE|ui-ux/00-updates/FIGMA-UPDATE|ui-ux/00-updates/MIGRATION-"

# Filter out exceptions
TEMP_FILES=$(echo "$TEMP_PATTERNS" | grep -vE "$EXCEPTIONS" || true)

if [ -n "$TEMP_FILES" ]; then
    echo "   ❌ Temporary documentation files detected:"
    echo "$TEMP_FILES" | while read file; do
        echo "      - $file"
    done
    echo ""
    echo "   📋 RULE: Temporary files MUST be in /tmp and deleted after use"
    echo "      ❌ NEVER commit: search results, planning prompts, audit findings,"
    echo "         status reports, bug fix summaries, design notes, cleanup files"
    echo ""
    echo "   ✅ CORRECT:"
    echo "      - Create temporary files in /tmp directory"
    echo "      - Delete temporary files after use"
    echo "      - Put permanent documentation in appropriate docs/ subfolders"
    echo ""
    echo "   📁 Temporary files should be:"
    echo "      - Linear issue search results → /tmp (then delete)"
    echo "      - Planning prompts → /tmp (then delete)"
    echo "      - Audit findings → Linear issues (not markdown files)"
    echo "      - Status reports → /tmp (then delete)"
    echo "      - Bug fix summaries → git commits and Linear issues"
    echo "      - Design notes → ADRs (docs/architecture/decisions/) or Linear"
    echo ""
    echo "   See: .cursor/rules/temporary-files-prevention.mdc"
    echo ""
    exit 1
fi

echo "   ✅ No temporary documentation files found"

# ============================================================================
# 4. Run lint-staged (linting, formatting, type checking)
# ============================================================================
echo ""
echo "4️⃣  Running lint-staged checks..."

pnpm lint-staged

if [ $? -ne 0 ]; then
    echo ""
    echo "❌ Lint-staged checks failed"
    exit 1
fi

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ ALL PRE-COMMIT CHECKS PASSED"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
